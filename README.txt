The Ledger Android
===================
This project wraps the supplied reminders.html UI in Android and adds native AlarmManager notifications.
Open in Android Studio and build the APK.
The app requests notification permission and uses exact alarms when permitted.
